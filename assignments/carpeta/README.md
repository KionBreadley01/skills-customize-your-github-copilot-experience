# 📘 Assignment: Carpeta

## 🎯 Objective

Create a basic assignment folder structure and verify the new assignment is registered in `config.json`.

## 📝 Tasks

### 🛠️ Create Assignment Folder

#### Description

Create an assignment directory named `carpeta` and add a `README.md` file using the assignment template.

#### Requirements
Completed assignment should:

- Include a `README.md` file in `assignments/carpeta/`.
- Follow the repository assignment template structure.
- Be registered in `config.json` under `assignments`.
